Hallo JPK,

danke für die schnelle Antwort :)
Ich habs heute mit den MacroBoards nochmal versucht. Es stellt sich heraus, das das GUI Änderungen an den vorhandenen MacroBoards nicht wirklich mit zu bekommen scheint. Ich musste jedes mal, wenn ich einen neuen Proxy erzeugt hatte, das GUI neu starten, damit der auftaucht.

Ich habe mich gestern und heute mal dran gesetzt, Wrapper für die einzelnen Clients zu bauen. Dabei häng ich gerade am folgenden:

Ein paar von den Anfragen per grpc haben als Feld den "idFilter" (quasi alles, was als Request die GetMultipleRequest hat). Wie genau funktioniert dieser Filter?
Ich habe das bisher so interpretiert, das der die vollständigen Ids von den Elementen, die mich interessieren, nimmt und dann nur die zurück gibt, die ich da angegeben habe. Teolweise funktioniert das auch, wenn ich beispielsweise bei selectExecutor einen idFilter angebe, wird der Executor ausgewählt. Wenn ich aber bei getAttachables einen idFilter angebe, antwortet der Kernel mit einem leeren Array. Bei AudioClient.requestAllSoundOutputInfos scheint der Kernel aber immer alle zurück zu geben, ohne Filter.
Könnt ihr mir da vielleicht weiterhelfen?

+ SpecialCueInsert Enum